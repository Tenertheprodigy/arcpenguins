import WLMessage from "@/components/WLMessage";

export const metadata = {
  title: "WL Application — Arc Penguins",
  description: "Whitelist applications for Arc Penguins are not open yet.",
};

export default function WLPage() {
  return <WLMessage />;
}
